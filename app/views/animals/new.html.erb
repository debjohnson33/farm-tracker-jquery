<h2>Add a New Animal</h2>
<%= form_for Animal.new, :url => farm_animals_path(@farm, Animal.new) do |f| %>

	<%= render 'animal_fields', f: f %>

	<h3>Select an Area:</h3>
	<%= collection_radio_buttons :animal, :area_id, @areas, :id, :name %><br><br>


	<%= f.submit %>

<% end %>